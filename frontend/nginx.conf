server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html;
    }

    location /logistics/ {
        proxy_pass http://backend:3000/logistics/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}